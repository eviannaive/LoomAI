
import 'package:flutter/material.dart';

class CharacterHubScreen extends StatelessWidget {
  const CharacterHubScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Character Hub Screen'),
    );
  }
}
